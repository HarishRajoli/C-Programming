#include <stdio.h>
int main (void)
{
    int a,b,add;
    printf("Enter the value of a:");
    scanf("%d",&a);
    printf("Enter the value of b:");
    scanf("%d",&b);
    add=a+b;
    printf("the addition of two number is :%d",add);
    return 0;
}